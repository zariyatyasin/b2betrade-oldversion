/** @type {import('next').NextConfig} */
const nextConfig = {
 
  images: {
    domains: ["res.cloudinary.com"],
  },
  // experimental: { 
  //   nftTracing: true 
  // }
  
};

module.exports = nextConfig;
